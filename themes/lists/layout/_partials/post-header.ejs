<header class="post-header">
  <% if (isIndex) { %>
  <a href="<%- url_for(post.path) %>" class="post-link">
  <% } %>
    <% if (isIndex) { %><h2 class="post-title"><%= post.title %></h2><% } else { %><h1 class="post-title"><%= post.title %></h1><% } %><% if (post.description){ %><span class="post-description"><%= post.description %></span><% } %>
  <% if (isIndex) { %>
  </a>
  <% } %>
</header>
